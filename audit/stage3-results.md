# Stage 3 — Fix results (caramel audit, incremental log)

Branch: `audit/fixes-2026-07-10` (cut from `audit/dev-2026-07-10` @ 9cac6fd). One commit per finding. Updated after each fix by the Opus lead.

## ★ FINAL SUMMARY (train complete @ 145fb10 — PR #111)

- **16 findings fixed in 19 commits** (15 planned F-001…F-015 excl. F-016, + F-017 discovered in-train by the new eval gate). F-016 = separate /one-root-compose initiative, untouched by design.
- **Suite: 0 → 305 tests** (290 app + 15 extension), all green; plus 40-case live eval suite (97.5% gate-green) and Playwright e2e (88 passed / 1 pre-existing known failure).
- **CI:** unit + oxlint + knip(truthful) + audit(high) + size-limit + ai-evals(armed pending secret) gates added; all quality legs green; the ONLY red anywhere = the pre-existing extension-smoke manifest-drift test (NF-03) + the by-design ai-evals missing-secret leg.
- **Empirical deltas (all measured, all lead-verified):** 3am blind spots 14 → 3-partial/0-open · onboarding tribal steps 5 → 0 (cold docs-only boot passes first try) · navigation behaviors 2/5 → 3/5 (functions 10/10 held) · change-trace 9/3/1 → 9/4/3 · raw status predicates 8 → 0 · any tokens 16 → 0 · vulns 89 (2C/47H) → 5 moderate · lockfiles 3 → 1 · false-green gates 3 (test/knip/prettier-tailwind) → 0.
- **E2E validation (Opus, real browser + real HTTP): 7/7 PASS** — incl. extension in real Chromium (zero ReferenceErrors) and the honest-failure UX on screen. Prod read-only check retired the COUPONS_DATABASE_URL concern; prod's real gap = missing OPENROUTER_API_KEY (NF-02).
- **Human handoffs:** in PR #111 body (secrets ×3, branch protection, extension release, RUNBOOK TODO(human) lines, TEMP CI trigger revert-at-merge).
- **Hallucination discipline:** every Haiku empirical mis-score was caught and corrected with line evidence (AM-11; OB-3/5/11/14) — corrections inline in the empirical-after files.

## F-004 — vitest baseline + characterization pins + CI unit job

- **Status:** COMPLETE · **Commit:** `517a1b1`
- **Plan-vs-actual:** all 7 steps as planned. Optional background.js pin SUCCEEDED (no document-skip): `_load.mjs` gained a real `onMessage.addListener` capture; pins confirm the F-002 wart (failed fetch → silent `{coupons:[]}`/`{supported:[]}`).
- **Checks:** app 9 tests green · extension 10 tests green · root `pnpm test` = 2/2 turbo tasks (was: 0 tasks false-green) · tsc clean · lint clean · knip 1 pre-existing hit (unused `CouponRow`, F-001 deletes it) · gate-bites proof: flipped assertion → root test RED, reverted → green (full turbo cache = byte-identical restore). Lead re-verified suite green post-commit.
- **Deviations (minor):** vi.mock paths use `@/lib/...` aliases (CR-1 relocation); `pnpm install` needed `CI=true` + `--no-frozen-lockfile` (pnpm 9→10 modules mismatch prompt — one-time; `.modules.yaml` now records 10.14.0); 794-line lockfile diff audited line-by-line = only vitest additions + peer-suffix bookkeeping, no pre-existing pins changed.
- **Empirical delta (measured):** `pnpm test` before = `turbo 0 tasks, exit 0` (false-green, OB-11); after = 19 real tests, proven red-on-flip. Onboarding blind spot OB-11 closed at the tooling level.
- **New-finding candidates:** (1) `checks-extension.yml` prettier step is ALREADY red on the baseline (cart-signals.js CRLF ×189 + index.html/manifest-firefox drift — matches Stage-0 tooling "13 unformatted"; F-015's repo-wide prettier-write will clear it; PR body must flag the leg as pre-existing-red until then). (2) Root husky lint-staged lints all packages with the ROOT eslint config instead of per-package configs. (3) vite-tsconfig-paths now redundant with Vite 8 native option (trivial, next cycle).
- **Notes for later fixes:** F-002 flips decrypt pin (c) + both background pins (reuse `getOnMessageListeners()`); F-003 flips rateLimit pins (a)/(b), unique-IP-per-test constraint; F-006 updates the 3 visibility pins in place; F-008 updates `_load.mjs` file list when splitting.

## F-005 — zod-validated env module + boot fail-fast + regenerated .env.example

- **Status:** COMPLETE · **Commit:** `e52dbee`
- **Plan-vs-actual:** all 7 steps done (step-7 regen pulled forward — drift pin stays green; accepted). zod@4.4.3 + server-only@0.0.1 pinned exact. Boot fail-fast proven LIVE: working .env → Ready + HTTP 200; empty DATABASE_URL → named-var crash at instrumentation load. All converted pages/routes re-verified live (8 pages 200, 2 extension routes 401/400 as designed). `.env.example` now documents 30 vars (was 19), port 58005, base URL 58000.
- **Checks:** 19 app + 10 ext tests green · gate-bite proof (schema loosened → pin red → reverted) · tsc/lint/prettier clean · knip = 1 pre-existing hit only (CouponRow) · `pnpm install --frozen-lockfile` (CI's command) succeeds post-commit · lockfile diff audited (zod dedup ripple only).
- **Deviations (accepted by lead):** (1) `tests/setup.ts` shims server-only under vitest (react-server condition absent outside Next bundler); (2) rateLimit.ts + decryptJsonData.ts keep dynamic `process.env` reads (F-004's vi.stubEnv pins depend on per-call reads; F-003 rewrites rateLimit next — vars stay in schema/.env.example); (3) **`.gitignore`'s `.env*` swallowed `.env.example` — it was NEVER git-tracked** (fresh clones had no env vocabulary at all; worse than F-005/F-010 found). Fixed with `!.env.example` + force-add; (4) BCRYPT_SALT_ROUNDS now validated positive int (fail-fast improvement within finding purpose); (5) EOL-only prettier churn on 5 unrelated files reverted pre-commit.
- **Empirical delta (measured):** boot on broken env before = boots then fails deep in request; after = named-var crash before Ready. `.env.example` coverage 19→30 vars and now actually ships in the repo.
- **New-finding candidates:** (1) audit other `.env*`-named shareable files for silent untracking; (2) checks-app.yml prettier leg pre-existing RED on 5 files (baseline drift — F-015's prettier-write clears; flag in PR); (3) oauth/route.ts recomputes baseURL twice (dup, next cycle).
- **Ruling for F-009 (lead):** the single knip hit (unused `CouponRow` type in couponsDb.ts) moves from F-001's scope into F-009 (one-line dead-type deletion; F-009 needs knip truthfully green at its gate). F-001 executor: it's already gone when you land.

## F-009 — delete Pages-Router fossils, truthful knip, oxlint + size-limit, husky knip+prisma

- **Status:** COMPLETE · **Commit:** `4e681b9` (37 files, +467/−503)
- **Plan-vs-actual:** all 10 steps done. Fossils (6 files incl. errorMiddleware.ts) deleted after per-file zero-caller grep; guard test written RED first (7 fossil-exists failures) → 10/10 green post-delete. cors/jsonwebtoken + @types dropped (lockfile diff = pure deletions). knip = ZERO issues (CouponRow deleted per lead ruling; its orphaned `DiscountType` ripple-deleted — verified unused, accepted: a fresh ignore entry would be the exact anti-pattern this finding kills). oxlint@1.73 root config wired (CI job + lint-staged front position). size-limit@12.1 with SOURCE-VERIFIED config fix: plan's `gzip:false` actually meant brotli-default — corrected to `brotli:false` (raw bytes = shipped bytes); budgets pass with 9–44% headroom. husky pre-commit + knip + prisma validate, dry-run proven. TEMP CI trigger for audit branch added with removal comment (CR-4). Branch protection = handoff commands only (JC-2).
- **oxlint reality (accepted deviation — plan said "trivial hits"):** 45 error-level hits triaged INDIVIDUALLY: ~35 dead-code fixes (18 unused catch bindings in shared-utils.js; 3 dead background.js functions with zero callers; dead helpers/vars/imports across app+extension), ~6 false-positive suppressions each with a one-line justification (content-script siblings called cross-file via manifest concatenation; Swift-invoked `show()` in apple-extension), 3 product-risk hits suppressed + escalated as new findings (below). Suppression syntax gotcha documented (`oxlint-disable-next-line` must be the LAST comment line above the target).
- **Checks:** knip 0 · tsc clean · oxlint 0 errors · size budgets pass · app 29 tests green (+10 guard) · ext 10 green · root 2/2 · next build clean (29 routes) · frozen-lockfile install clean · husky dry-run all 4 gates pass. **e2e baseline established (warm + serial methodology; box shared with 30+ containers):** 81 passed / 2 failed PRE-change and identically post-change — extension-smoke manifest-pattern drift (deterministic, pre-existing) + sources-page intermittent flake (reproduces standalone, pre-existing).
- **Empirical delta (measured):** knip gate before = green-while-blind (fossils whitelisted, 1 real hit ignored); after = truthfully zero with zero unjustified ignores. Dead `withAuth` trap for cold agents is gone (files deleted). New gates: oxlint (45 real hits at intro), size-limit (raw budgets), husky knip+prisma.
- **New-finding candidates:** (1) `WhyNot.tsx` two content arrays never rendered (dead copy or missing section — product call); (2) `suggestion-form.tsx` has NO input element and `setUrl` never called — form can only submit its initialValue (looks like a real bug); (3) `coupons-section.tsx` `setStoreOptions` never called — store filter permanently `[]` (looks like a forgotten line); (4) extension-smoke e2e failure = real manifest-matches vs supported-sites drift (deterministic); (5) sources-page e2e intermittent flake (possibly local coupons-DB seed gap); (6) stale tracked `apps/caramel-app/pnpm-lock.yaml` trap (F-014 deletes).
- **Notes:** F-001 — couponsDb.ts type cleanup already done here. F-007 — fossils gone, build wrapper fresh. F-008 — size-limit group path array + ~8.3KB extra headroom. F-011 — fold branch-protection command block into RUNBOOK. F-015 — ext prettier drift now only index.html + manifest-firefox.json.

## PR #111 — Wave-1 CI verdict (recorded by lead)

- App CI: schema-drift ✓ prettier ✓ lint ✓ knip ✓ typecheck ✓ **unit ✓ (new)** **oxlint ✓ (new)** · E2E leg: **81 passed / 1 failed / 1 flaky** — the 1 failure is the PRE-EXISTING deterministic extension-smoke manifest-drift test (same as local baseline); flaky retried green. **Zero Wave-1 regressions.** Extension CI: fully green.
- Corrections to earlier caveats: the "pre-existing prettier red" seen locally does NOT manifest in CI (linux checkout normalizes the CRLF drift — it's a Windows-checkout artifact). CI e2e WebServer logs confirm `caramel_coupons` DB has never existed in CI (F-001 territory, pre-existing).
- App CI has been red on `dev` pushes since before the audit (e2e-push variant) — the audit train did not cause it.

## F-003 — server-only expire secret + identity-based rate-limit exemption; keyless public supported-stores

- **Status:** COMPLETE · **Commit:** `8231e1d` (10 files)
- **Plan-vs-actual:** all 5 steps as planned. Pins-first on current key behavior → atomic swap (env schema EXTENSION_API_KEY→COUPONS_ADMIN_SECRET optional; expire = fail-closed bearer; rateLimit `isExtensionClient`→`isTrustedServer` reading env module; supported-stores keyless behind read limit) → pins flipped/extended → extension stops shipping/sending the key. Design addition per CR-8: `isTrustedServer` EXPORTED and reused as expire's auth gate — one checker, ready for F-007's wrapper.
- **Checks:** app 43/43 green (was 29) · ext 10/10 · root 2/2 · tsc clean · oxlint 0 errors · knip 0 · lockfile untouched · **live curl against real dev boot:** expire no/wrong/stale-key bearer→401, correct bearer→200 `{count:0}`; supported-stores with and without stale key behaves IDENTICALLY (both reach the coupons query layer; local 500 = `database "caramel_coupons" does not exist`, the already-filed F-001 gap — live confirmation, not a regression).
- **Deviations (accepted):** module-level frozen env read discovered in old supported-stores gate (one untestable sub-case dropped; code path deleted anyway); 3 prose comments rephrased so the EXTENSION_API_KEY grep is unambiguously zero; oxlint-disable placement made prettier-stable; one orphaned dev-server killed by specific PID (hygiene per rules).
- **Empirical delta (measured):** the shipped public key before = authorizes a destructive mutation + bypasses all throttling; after = confers NOTHING server-side (proven at the HTTP layer), extension no longer ships any privileged value, exemption requires the server-only bearer.
- **Human/ops handoff (PR body):** set `COUPONS_ADMIN_SECRET` in Dokploy prod+dev; delete `EXTENSION_API_KEY` from Dokploy env (now unused); ship extension release at convenience (old clients degrade gracefully — no deadline).
- **New-finding candidates:** none.

## F-002 — route-error Sentry helper + honest decrypt/extension failures

- **Status:** COMPLETE · **Commit:** `014a55f` (21 files, +408/−85)
- **Plan-vs-actual:** all 6 steps as planned. 3 flip-pins written RED first (fake-empty, DecryptError-undefined, Sentry-called-0-times) → helper `src/lib/api/handleRouteError.ts` (+6 unit tests) → all 14 catch sites across 13 route files converted (classify-cart keeps dynamic OpenRouterError→502 + dynamic message; suggest gained its `(error)` binding) → `DecryptError` + `MaybeEncrypted<T>` types, `||` passthrough byte-preserved, sources/page.tsx hardened (sonner toast — existing dep; distinct "Couldn't load sources." vs "No sources found." rows) → background.js non-2xx now `{error:'HTTP <status>'}` matching the classifyCart convention; popup renderLoadError UI pin ACHIEVED via real listener chain (no document-skip).
- **Checks:** app 49/49 (+6) · ext 11/11 (+1 popup pin) · root 2/2 · tsc clean · oxlint 0 · eslint 0 errors · knip 0 · next build 29 routes · lockfile untouched · **live curls:** /api/coupons + /api/sources + /api/extension/supported-stores all return the NEW `{error}` + `x-request-id` envelope (500s = the known local caramel_coupons gap, surfaced honestly now).
- **Deviations (accepted):** helper gained `headers?: HeadersInit` passthrough — oauth's catch site sends corsHeaders and dropping them would break cross-origin error reads (F-007 informed: wrapper needs same passthrough); catch-site line re-anchors (F-003 shifts); classify-cart status expression folded into the helper call; Sentry route pin colocated in handleRouteError.test.ts as a second describe; used the pre-existing 58000 dev server for live curls (leftover from an earlier executor — lead killed PID 786912 after wave-step; port verified free).
- **Empirical delta (measured):** caught route errors before = console.error + generic 500, invisible to Sentry; after = Sentry.captureException with route/method/requestId tags + one envelope + x-request-id (proven live). Extension failure before = renders as "no coupons"; after = distinct error state driving the existing renderLoadError UI (pinned in popup.test.mjs).
- **New-finding candidates:** none beyond items already routed (body-parse swallows → F-007, envelope unification → F-006/F-007, pattern lint-ban → future).

## F-001 — coupons-DB health probe + typed read boundary + schema-drift check

- **Status:** COMPLETE · **Commit:** `0359cb4` (21 files, +1434/−78) · **+ lead amendment `19b91ef`** (see below)
- **Plan-vs-actual:** all 6 steps done. 8 zod row schemas + `parseCouponRows` + `pingCouponsDb` in couponsDb.ts (compile-time-only generic casts REMOVED — schemas are the type source now); boundary wired into all 9 read sites / 12 queries; health route dual-probe `{status, checks:{auth_db, coupons_db}}`, 200/503/401 contract preserved; drift script (testable core + CLI) + `check:coupons-schema` + dispatch-only workflow with loud secret pre-flight.
- **Checks:** 94/94 tests (45 new) · tsc/lint/knip clean · oxlint 0 (8 own errors found+fixed during work) · **live health probe against real dev boot: bearer → 503 `{auth_db: ok(2081ms), coupons_db: error("database caramel_coupons does not exist")}`** — the 3am blind spot is now a red monitor, proven on real infrastructure · drift script proven BOTH mocked (5 tests) and live against the auth DB (exits 1 listing all 34 expected columns missing — the failure path fires correctly) · `gh secret list`: COUPONS_DATABASE_URL secret ABSENT (ops handoff, PR body).
- **Deviations (accepted):** source+tests built in one continuous pass instead of strict two-phase pins (these are new-behavior tests, not flips; the full-suite run caught the one real regression — two earlier test files' couponsDb mocks needed importActual re-plumbing, assertions untouched); zod v4 coercion trap dodged empirically (`z.coerce.string()` turns null into "null" — used union+transform); 8 z.infer type aliases deleted (knip/oxlint dead-export chain; schema constants remain the source); one commented `as Coupon[]` cast reconciling two pre-existing type declarations on validated data.
- **Empirical delta (measured):** health before = SELECT 1 on the wrong DB, green during a total coupons outage; after = per-DB probe, coupons outage → 503 + named details (proven live). Schema drift before = silent wrong data; after = loud labeled throw → Sentry via F-002's helper (route-level drift test proves 500-not-silent).
- **New-finding candidates:** (1) COUPONS_DATABASE_URL template `?schema=public` trap — F-005 train regression, FIXED by lead amendment; (2) local prettier drift on 2 untouched files (known, CI-green, Windows CRLF artifact); (3) drift-workflow secret missing (ops handoff).

## F-011 — operability runbook, error boundary, trace correlation, smoke

- **Status:** COMPLETE · **Commit:** `ef85860` (17 files) — closes Wave 2
- **Plan-vs-actual:** all 8 steps as planned. RUNBOOK.md at root (dashboards from real configs; Dokploy/Uptime-Kuma unknowns as TODO(human); branch-protection block folded verbatim from PR body); error.tsx + global-error.tsx per current Next 16 + Sentry docs (context7-verified, not memory); `automaticVercelMonitors` removed (zero refs remain); openrouter.ts gains X-Request-Id + generation-id span attrs (NO hand-rolled span — auto fetch instrumentation confirmed for @sentry/nextjs v10); couponsDb `connection.application_name` verified against porsager's shipped .d.ts; `pnpm smoke` script live-proven (/ PASS, health FAIL on the honest local coupons-down state, exit-1-on-first-failure works); README pointer.
- **Checks:** app **126/126** (+32) · ext 11/11 · tsc/eslint(both cwd contexts)/knip clean · oxlint 0 new · next build 29 routes ×2 · lockfile diff = exactly @testing-library/react@16.3.2 + jsdom@29.1.1 (exact-pinned) + closure · dev server stopped by own task PID, port verified clear.
- **Deviations (accepted):** vitest include widened to `*.test.{ts,tsx}` (plan's own .tsx tests were silently uncollected — necessary); file-scoped eslint flat-config override for global-error.tsx's deliberate raw `<a>` (inline disable kept getting stripped by the oxlint→eslint→prettier chain; `**/`-glob verified from BOTH config-discovery contexts).
- **Empirical delta:** measured by the Haiku 3am re-run (below).
- **New-finding candidates:** RUNBOOK TODO(human) punch list (Dokploy names, alert routing, smoke wiring into deploy hook) — ops handoffs, in PR body.

## Wave-2 empirical re-run — 3am incident (Haiku, lead-verified)

- `audit/empirical-3am-after.md`: **11 of 14 blind spots CLOSED, 3 PARTIAL (AM-8 log-gating, AM-9 deploy automation, AM-14 coverage sweep), 0 fully open** — from 14/14 at baseline. Lead correction applied: the Haiku's AM-11 "still open" verdict restated the BEFORE state and failed verification (repo-wide grep: the key is gone from all live code; F-003 proven live) — re-ruled CLOSED, footnote in the file. Remaining gaps = RUNBOOK TODO(human) ops facts + CI deploy/smoke automation.

## F-006 — single coupon-domain module + generated extension constants + raw-status ban gate

- **Status:** COMPLETE · **Commit:** `5777695`
- **Plan-vs-actual:** all 8 steps per the drift-ruling table. `src/lib/coupons.ts` (STATUS_TABLE → vocabulary/predicates/STATUS_META); 3 fragment factories in couponsDb.ts (postgres@3.4.9 `.strings`/`.args` API verified from installed SOURCE before use); 6 verbatim sites + 2 ranking sites unified; filters = the ONE pre-approved behavior change (broadens 'valid'→visible; staging-verifiable only; 1-line rollback documented); stats = `verifiedCensusSql()` behavior unchanged; coupon-card on STATUS_META; codegen → COMMITTED `coupon-constants.generated.js` prepended in both manifests + index.html; popup rebound to `window.CaramelCoupons.*`. **Both gates proven RED→green** (sync test via planted stray line; ban gate via planted tokens in an app route AND background.js, clean reverts diff-verified).
- **Checks:** app **152/152** (+26) · ext **15/15** (+4) · root 167/167 · tsc/eslint/knip/prettier clean · oxlint 0 errors (133 warnings in touched files verified pre-existing line-by-line) · size-limit 83.82/92 KB.
- **Deviations (accepted):** vi.mock importActual-closure gotcha discovered (mocked couponsSql does NOT intercept fragment factories' internal calls — pins redesigned to inspect real `.strings`/`.args`, STRONGER than planned; gotcha documented in-file); 2 extension harness beforeAll updates (generated file must load first — direct consequence); +2 extension test files beyond plan (end-to-end wiring proof incl. reactivity, replacing the live-server-dependent test-extension.mjs which needs dev server + coupons DB + auth user — not available; CI doesn't run it either); ban-gate allowlist exception for popup's pre-existing restriction-detail SENTENCES (presentation copy with no app-side counterpart — commented); `.size-limit.json` untouched per CR-2 (F-008 owns the path array; generated file is 1.4 KB, immaterial — verified by real run).
- **Empirical delta (measured):** `status IN (` raw predicates in src/ before = 6 verbatim + 2 drifted variants; after = 0 (grep-proven) — one module + named fragments; app↔extension constants now generated from one source with a sync gate; ban gate active.
- **New-finding candidates:** Firefox manifest staleness INDEPENDENTLY confirmed (missing cart-signals.js AND caramel-content.css; matches-list drift vs chrome) — strengthens the parked Firefox-parity finding.

## F-007 — declarative withRoute pipeline + centralized extension OAuth session mint

- **Status:** COMPLETE · **Commit:** `125827b` (21 files: 16 routes, 2 new lib modules, 3 new test files)
- **Plan-vs-actual:** wrapper + preflight built (apiKey concern = the exported `isTrustedServer`, per CR-8 — no second checker); Batch A read-only → Batch B mutations (supported-stores keyless per tree, CR-8) → Batch C auth/oauth LAST with the safety sequence exactly as planned: oauth pins green on unmodified route (17 green / 3 correctly-red-for-future-state) → `mintExtensionSession` extracted → SAME 17/3 split = wire-identical proof → wrapped login/authorize/redirect/exchange → all 3 flipped green. 400→422 transitions limited to genuinely-missing-field cases per plan.
- **Checks:** app 202/203 (the 1 fail = F-006's sync test, broken AT HEAD before this diff — see amendment below) · ext 15/15 · tsc/eslint/knip/prettier clean · oxlint 0 new · **live HTTP verification of the pipeline:** real signed Google authorize URL; 422-vs-400 distinction correct; origin-gate 403 fires; OPTIONS 204+CORS on authorize/exchange; expire fail-closed 401; coupons-DB routes keep exact pre-existing degraded messages. Dev server killed by own PID; shared containers untouched.
- **Deviations (accepted):** lenient schemas use z.unknown() (stronger zero-behavior-change guarantee); plan-internal contradiction (sources 422 in table vs "preserved" in prose) resolved in the table's favor (plan self-declares the table authoritative); `auth:'session'` dynamic-imports better-auth (keeps 15 non-auth routes off the heavy graph); 3 now-dead manual field checks removed as direct consequence of wrapper guarantees; authorize/login INNER catches left as-is (outside F-002's converted set — parked as new finding).
- **Empirical delta (measured):** cross-cutting posture before = per-route hand-rolled/absent (5 origin impls, 3 CORS copies in one file, 4 unthrottled sensitive routes); after = one declarative wrapper on all 16 routes, coverage visible in each route's options literal, oauth mint = ONE module (was 2 divergent branches).
- **New-finding candidates:** authorize/login inner catches bypass handleRouteError (no Sentry/x-request-id); `mintExtensionSession` returning-user response omits freshly-updated name/image (pre-existing, preserved byte-identical); `cors:'public'` mode ready-but-unused (like auth:'session', tested); F-006 sync-test defect (fixed by amendment below).
- **Pre-ship gate (PR body):** verify prod `CHROME/FIREFOX/SAFARI_EXTENSION_ORIGIN` list the shipped extension IDs BEFORE merge — the oauth-exchange CORS tighten breaks the live extension otherwise. Full extension-in-browser OAuth E2E deferred to the dedicated E2E phase.

## F-008 — split shared-utils.js into 6 cohesive content-script files (behavior-identical)

- **Status:** COMPLETE · **Commit:** `2d9b962` · **+ lead amendment `12d675f`** (below) — closes Wave 3
- **Plan-vs-actual:** census re-anchored by symbol (one boundary sits at an UNbannered comment — caught by cross-checking the symbol-ownership list, not banner regex); 6 files cut in one pass (lead-approved simplification); both manifests + index.html + test harness + test-extension.mjs eval list updated; size-limit group path updated per CR-2 (+ coupon-constants.generated.js added — F-006 had left it out).
- **The plan's thesis had one real hole, caught and fixed:** `_isDevInstall` is called at MODULE-EVAL time from caramel-base.js (top-level log fallback + CARAMEL_ALLOWED_ORIGINS init) — cross-file hoisting doesn't exist between separate scripts, so the naive cut would have thrown ReferenceError on every store page. Found via fresh-V8-realm prefix-load checks BEFORE shipping; fixed by verbatim 15-line relocation into caramel-base.js (documented in both file headers). Plus 12 oxlint cross-file-usage disable comments following the pre-existing startCheckoutDetection convention.
- **Cat-diff proof (mandatory):** concatenation in load order, headers stripped, reproduces the pre-split file byte-identically except the ONE relocated block (verified present exactly once in each; excising it → byte-identical true) + comment-only insertions; full 174-line diff eyeballed AND programmatically classified (10 non-comment added lines == the 10 relocated lines, byte-equal).
- **Checks:** ext 15/15 pre AND post · vm prefix-load 9/9 clean · **REAL Chromium end-to-end: applyCoupon flow green ($100→$90 price assertion) via a standalone mirror of test-extension.mjs step 8** · size-limit 91.13/92 KB PASS (headroom now ~0.9 KB — flagged) · eslint/prettier/oxlint clean · app untouched by the split commit.
- **Empirical delta:** measured by the navigation re-run (below).
- **New-finding candidates:** (1) apple-extension Xcode project references the deleted file — investigated: DEAD WEIGHT, not a regression (release pipeline regenerates the project from dist/ via safari-web-extension-converter; the checked-in one predates UI-helpers/cart-signals too) — recommend deletion next cycle; (2) size-limit content-scripts headroom thin (0.9 KB); (3) the cross-file oxlint disable pattern (13 instances) deserves a config-level solution eventually.

## Lead amendment — `12d675f` (fix(F-008) follow-up)

- The app's `e2e/extension-smoke.spec.ts` hard-coded `shared-utils.js` in its file-inventory assertion → CI e2e would have gained a NEW red on the F-008 push. Gap caused by the lead's own "don't touch the app" instruction to the F-008 executor. Lead fixed pre-push: inventory now lists the 6 split files + coupon-constants.generated.js (15/15 existence-verified by script; tsc clean). Pushed together with 2d9b962 — no red-CI window.

## Wave-3 empirical re-runs (Haiku) + CI verdict

- **Change-trace** (`audit/empirical-change-trace-after.md`): expiry-badge 9/10 → 9/10 (was already good); lifetime-savings 3/10 → 4/10 (withRoute gives the auth/error scaffolding; the missing user↔activity persistence is product work, not debt); per-store success-rate 1/10 → **3/10** (SQL/badge duplication GONE — route + store page consume the same factories/constants; remaining drag = external Python DB ownership, the F-016/next-cycle boundary).
- **Name-only navigation** (`audit/empirical-navigation-after.md`): behaviors 2/5 → **3/5**; functions 10/10 → 10/10. Remaining misses are honest: the price-comparison loop lives in coupon-runner.js (orchestrator) vs the predicted coupon-apply.js, and coupon creation lives in the out-of-repo Python service (no in-repo name can fix that).
- **CI @ 12d675f:** all quality legs green (unit/oxlint/knip/typecheck/lint/prettier/schema-drift); e2e **88 passed / 1 failed** — the one failure is the PRE-EXISTING extension-smoke manifest-drift test (unchanged since baseline); the F-008 inventory amendment's new existence tests all pass. Zero train regressions in CI through three waves.

## F-013 — type all exported any surfaces + no-explicit-any at error

- **Status:** COMPLETE · **Commit:** `48f5142` — opens Wave 4
- **Plan-vs-actual:** step-0 re-census found 16 live tokens / 5 files (decryptJsonData already clean via F-002; F-007's new modules clean). Pins FIRST for the two never-tested modules (13 cases green on unmodified code) → exported surfaces to `unknown` → coupon-filters `StylesConfig<Option,false>` (verified against installed react-select 5.10.2) → interior (OAuthStatePayload interface; sources/page.tsx) → `@typescript-eslint/no-explicit-any: error` in root flat config (resolves via next's bundled plugin v8.47.0; RULE PROVEN FIRING on a scratch violation, exit 1).
- **Checks:** app **216/216** (+13) · ext 15/15 · tsc/-r type-check clean · eslint 0 · knip clean · oxlint clean · `next build` 0 errors, 29 routes via routes-manifest (not cache) · zero suppressions added (repo still has none).
- **Deviations (accepted):** TS2698 forced `...(data ? {data} : {})` rewrite — runtime-identity property-tested across 11 falsy/truthy values + pinned; sources/page.tsx stale `Source` interface said string where the server produces number — resolved under the plan's pre-approved drop-the-cast branch (file-local type, zero render change; disclosed fully); eslint rule scoped to `**/*.{ts,tsx}` files-block matching next's plugin registration (verified no plugin-resolution error on .mjs configs); one self-owned lint nit merged; import-line convention preserved.
- **Empirical delta (measured):** live `any` tokens in app src: 16 → 0 with an ERROR-level gate that provably fires; two exported boundary modules gained their first-ever tests.
- **New-finding candidates:** `encryptJsonServer` has ZERO callers repo-wide (fossil candidate for next cycle); 3 pre-existing oxlint style warnings in touched files (unrelated, untouched).

## F-015 — remove stray/broken artifacts, rename to caramel-app, activate tailwind sorting, repo-integrity gate

- **Status:** COMPLETE · **Commit:** `f34ceab`
- **Plan-vs-actual:** integrity gate written FIRST and proven RED on exactly the amazon.js ghost (36 tests, 1 failure) → entry removed → 35/35 green. Stray `verify_test_small3.txt` deleted; `nul` gitignored (allowlist test deliberately uses `git ls-files`, not fs scan, so the untracked local `nul` can't false-fail it). **Rename: CR-6 re-grep found 16 refs / 8 files** (plan knew 5 — the other 10 were added by F-004/F-006/F-009/F-010/F-011 commits) — all updated; generated file REGENERATED via its generator (sync-test invariant preserved); zero lockfile refs/diff, no install needed. Prettier key fixed; activation proven (prettier-check: 0 → 30 flagged files pre-write, then all green). CI wiring = the existing unit task (vitest home).
- **Checks:** app **251/251** (+35) · ext 15/15 · CR-6 gate 0 hits · tsc/lint/knip/oxlint/prettier(both packages) clean · size 91.12/92 KB · generated-file SHA identical across the repo-wide prettier pass · **husky no-op PROVEN by tree-hash equality** (staged write-tree == committed tree) · test-extension.mjs not runnable (needs live server + seeded user; NOT in CI gate; service worker + manifest list loaded fine before the dev-URL fetch step — honestly reported, not claimed).
- **Deviations (accepted):** allowlist grown to the current 13 root files (RUNBOOK.md, .oxlintrc.json — CR-5); tsc caught an es5-target iterator-spread bug in the executor's own new test (fixed with Array.from, tsconfig untouched); `.prettierrc.json`'s own latent CRLF violation normalized in-commit (the file was being edited anyway); execFileSync over shell-exec per security guidance.
- **Empirical delta (measured):** Firefox manifest no longer references a nonexistent script; package name no longer lies (16 refs consistent); tailwind-aware formatting actually runs (was silently dead since introduction); future strays/manifest ghosts fail CI (35-test gate).
- **New-finding candidates (consolidated with earlier):** apple-extension pbxproj amazon.js refs (Safari build — but see F-008's investigation: the release pipeline regenerates the Xcode project, checked-in one is dead weight → recommend deletion); Firefox manifest missing cart-signals.js + css key (parity); root tailwind.config.js empty stray; NO CI check covers root-level loose-file formatting (how the .prettierrc CRLF drift survived — candidate: root prettier-check step).

## F-014 — patch crit/high vulns, drop dead deps, pin all deps to one root lockfile, CI audit gate

- **Status:** COMPLETE · **Commit:** `93cde28` (12 files, −12,321 lines mostly the two deleted nested lockfiles)
- **Plan-vs-actual:** all 7 batches done. **Vulns (fresh audit, not tool-sourced): 89 (2 crit / 47 high / 32 mod / 8 low) → 5 (0 / 0 / 5 / 0)** — direct in-major bumps (better-auth 1.6.23, next 16.2.10, sentry 10.65, sass, formik, web-ext 8.10) + 5 minimal-version pnpm.overrides (shell-quote closes the critical; scoped `vitest>picomatch` to avoid force-majoring coexisting picomatch lines). 71 ranges pinned via uncommitted script from live resolved versions; **resolution-neutrality proven structurally: packages-block diff = 0 added / 44 removed (pure dedup) / 0 changed**. Both nested lockfiles deleted; extension workflows repointed to root lockfile + frozen-lockfile. Audit gate at `high` (plan's rule: residual crit+high = 0) — proven exit-0 pre+post. `deps-pinned.test.ts` guard proven RED→green (planted caret caught).
- **packageManager ruling (evidence-backed):** live CI logs show BOTH action-setup majors resolve pnpm 9.0.0 FROM the field — CI's real major is 9; bumping to 10 would be a behavior change out of scope. Field unchanged; lockfileVersion 9.0 stable all session under local pnpm 10.
- **Material deviation (accepted):** `@better-auth/prisma-adapter` exposed as FULLY DEAD by the Batch-B bump (app imports the adapter bundled inside better-auth; verified true at pre-work HEAD) → deleted with its vulnerable subtree rather than patching a corpse; knip-0 invariant maintained.
- **Checks:** app **258/258** (+7 guard) · ext 15/15 · tsc/knip/oxlint/eslint/prettier clean · next build 29 routes · prisma generate/validate/migrate-deploy pass (empirical proof the forced `effect` override doesn't break Prisma config — flagged as the diff's riskiest line) · frozen-lockfile clean · e2e ×7 runs: no new failure CLASS (pre-existing extension-smoke fail + cold-compile-under-load flake on this 30-container box; one clean 88/1 run matches baseline exactly) · extension rsync build not runnable on Windows (pre-existing env gap; CI ubuntu covers it).
- **Majors known-debt (next cycle, 13 items):** prisma 6→7, TS 5.9→7, tailwind 3→4, eslint 9→10, knip 5→6, lint-staged 16→17, argos cli/playwright, @react-email/render, @types/node, react-infinite-scroll-component, vite-tsconfig-paths, web-ext 8→10.
- **New-finding candidates:** 5 residual moderates (postcss has a trivial in-major bump); pnpm-10 blocked postinstall for @sentry/cli locally (CI unaffected); NO scheduled re-audit (gate fires on push/PR only — nightly `schedule:` would catch new CVEs on unchanged pins); local e2e flake band on this box broader than the documented baseline (same mechanism).

## F-012 — cart-classifier eval suite + ai-evals CI gate + zod output schema

- **Status:** COMPLETE · **Commit:** `e6700f9` (13 files, +1963/−27) — with the plan's ≥0.85 baseline criterion UNMET for the right reason (see F-017)
- **Plan-vs-actual:** all 8 steps. 12 characterization pins on parseResponse (byte-exact neutrality cross-proven side-by-side old-vs-new on 12 inputs); zod `classificationSchema` shipped (safeParse + re-throw preserving exact error text; all fields optional — zod v4 rejects absent keys on bare z.unknown, caught empirically); 40-case fixture dataset; 5 deterministic scorers; separate eval config (`pnpm eval`, dependency-free env loading via `process.loadEnvFile()` per CR-9); `ai-evals.yml` (PR path-filtered incl. env.ts, nightly+auto-issue, dispatch, fork guard, loud missing-secret error; OPENROUTER_MODEL deliberately NOT pinned in workflow env — the workflow must never become its own drifting pin); knip `project` extended to evals/\*\* (proven catching a planted dead export); codify block in evals/README (no CLAUDE.md yet — F-010/Stage-4 relocates).
- **THE HEADLINE — the gate caught a real production bug on day one:** baseline = **5% primary-match (2/40)**. Root cause diagnosed with raw-API evidence: `maxTokens:120` + gpt-5-mini (reasoning model) → hidden reasoning eats the whole budget → `finish_reason:"length"`, content null → "empty response" on ~95% of calls. Same cart at 600 tokens → clean correct JSON. Gate NOT lowered (per plan's own rule). → promoted to **F-017**, fixed in-train (below).
- **Second HIGH (ops):** live Dokploy app has NO `OPENROUTER_API_KEY` — classify-cart is fully non-functional in prod TODAY (pre-existing; verified against the live app config). Also noted unverified: the same env block appeared to lack `COUPONS_DATABASE_URL` — E2E phase disambiguates read-only (env layering vs genuinely broken).
- **Checks:** app **290/290** (+32) · eval files proven NOT collected by unit glob · tsc/knip/prettier/oxlint(4 warn-category, matching repo convention)/doctor/prisma-validate clean · secret re-verified ABSENT · red-proof: free scrambled-label pipeline test (decisive) + live scramble capped to 8 cases (exit 1, named failures; cost-capped per brief) · total live spend ≈ cents.
- **New findings:** F-017 (below); prod env provisioning (PR-body handoff: add OPENROUTER_API_KEY to Dokploy WITH the CI secret).

## Lead ruling — F-017 created and fixed in-train

- New finding (High): cartClassifier token starvation, eval-caught. Micro-plan `audit/plans/PLAN-F-017.md`, self-approved under gate collapse. Rationale: shipped user-facing feature ~95% dead; a red-at-birth eval gate invites gate deletion and defeats F-012; the verification harness already exists making this the cheapest-risk fix in the train. Executed by the resumed F-012 agent (holds the diagnostics). Doctrine note: "mid-fix discoveries = next cycle" is overridden here by the gate-integrity argument; logged as the deliberate exception.

## F-017 — size cartClassifier token budget for reasoning models (eval-gated) [NEW FINDING, fixed in-train]

- **Status:** COMPLETE · **Commit:** `09d8dbb` (3 files) — executed by the resumed F-012 agent per lead ruling
- **Result:** `maxTokens: 600` shipped on measured evidence — candidates: 120 (baseline, 5%, 38/40 truncations) · 400 (95%, 0 truncations, but 2/40 timeout-aborts at p95 7005ms — NOT latency-safe) · **600 (97.5%, 0 truncations, 0 timeouts, p95 5965ms = 15% margin under the 7000ms wall, $0.000391/call — statistical cost tie with 400)** · 800 skipped with evidence (600 already 2x observed-max completion headroom). Reasoning-effort alternative not triggered (its gate condition — p95 threatening timeoutMs — didn't fire). Green ×2 on the shipped value (sizing run + official gate command exit 0). Cost figures exact from OpenRouter usage.cost ($0.0156/full run). 120 live calls, under the ≤160 budget.
- **Scoreboard:** red F-012 baseline row preserved on top (annotated), green official row below: 2026-07-11 · gpt-5-mini · 97.5% (39/40) · 100% schema-valid · p50 3291 / p95 5965ms · GREEN at maxTokens:600 — the before/after is the permanent regression-proof the v5 rules require.
- **Checks:** 290/290 unit green (zero pin flips, as the micro-plan predicted) · tsc/knip/prettier/oxlint clean · lockfile untouched · README status deviation accepted (stale "currently RED" line corrected — leaving it would be the exact stale-doc defect the rules ban).
- **Eval-maintenance note (parked, deliberately NOT fixed mid-F-017 = gate-rigging):** the 3 residual failures are junk-fixture confidence-calibration misses (model says `other` correctly but with 0.8–0.95 confidence vs the fixture's ≤0.6 cap) — a fixture-calibration review item for the next eval pass.

## F-010 — onboarding docs rewritten to the post-fix reality (FINAL fix of the train)

- **Status:** COMPLETE · **Commit:** `989fb6d` (4 doc files, zero code/config) · **+ lead amendment `0a806fd`** (fix(F-005): .env.example credentials) · **+ consistency commit `145fb10`** (fix(F-010): LOCAL-DEV note)
- **Plan-vs-actual:** Step-0 observed-values table done (all landed facts verified; 2 plan assumptions corrected: Safari icon scripts actually live in `.github/workflows/scripts/` and only run in the release workflow; the committed `apple-extension/` is never CI-regenerated — the release pipeline converts into an ephemeral project). Migration command PROVEN against an isolated empty postgres:16 container (3 migrations, 0 prompts). README Getting Started + secrets-provenance table (every var classified); LOCAL-DEV rewritten (two-DB topology, degraded mode with exact live-verified error text, 4 troubleshooting entries); both app/extension READMEs replaced (~16 lines each; the old app README was literally corrupted/binary). **Pre-flight: full verbatim run of the executor's own docs in a clean clone** — install → env → compose → migrate → dev (HTTP 200) → tests → one-line change through both gates → clean revert. `docker compose down` deliberately skipped (shared infra) — documented.
- **Two REAL findings from the pre-flight:** (1) **`.env.example` shipped `postgres:postgres` while compose creates `caramel:caramel_password`** — the literal copy-paste path failed P1000 on the MAIN database. F-005-train-regression → lead amendment `0a806fd` fixes the example at source (+ README/LOCAL-DEV consistency in `0a806fd`/`145fb10`). (2) **No `.gitattributes`** → fresh Windows clone with default autocrlf breaks the byte-exact generated-file test (1/305) — PARKED as a next-cycle finding (repo-wide normalization deserves its own pass); workaround documented in LOCAL-DEV troubleshooting.
- **Checks:** main-tree suite 290+15 green · clean-clone pre-flight green end-to-end · root allowlist stayed green (no new root files) · `pnpm eval` verified structurally (not run — real spend, not required by pass criteria) · shared containers untouched.

## F-010 validation gate — cold-agent onboarding trace (Haiku, lead-verified): **PASS**

- Fresh zero-context Haiku, docs-only, clean clone: **Goal 1 boot→HTTP 200 ✓ · Goal 2 tests (289+1 documented CRLF) ✓ · Goal 3 one-line change through documented gates ✓ · 0 new TRIBAL findings.**
- Lead verification corrected FOUR mis-scored baseline rows in the Haiku's mapping table (OB-3/5/11/14 — each disproven with line evidence; two repeated the before-state, one self-contradicted, one misread the doc-split rule). Final: **12/12 checked baseline gaps fixed or doc-covered** (OB-10/13 not exercised but fixed by F-010/F-015, inspectable). `audit/empirical-onboarding-after.md` carries the corrections inline.
- Baseline compare: BEFORE = partial boot, 5 tribal-knowledge steps, false-green tests; AFTER = docs-only full boot first try, zero tribal steps, real suite.

## Executor amendment — `e947a1d` (fix(F-006) follow-up, sync-gate hook survival)

- Defect (found by F-007's executor, fixed by the RESUMED F-006 executor): the sync test was red at HEAD because husky's ROOT-invoked `prettier --write` reformatted the committed generated file at commit time — the extension-scoped `.prettierignore` never applied to it, and raw `JSON.stringify` output could not match. Fix: generator now emits prettier-FORMATTED output via the API (config resolved from the OUTPUT path — the hook's own resolution), regeneration reproduced HEAD's bytes exactly (hook fixed-point proof), idempotence proven against the hook's exact invocation, RED-proof re-run, scoped `.prettierignore` removed (created solely for this; prettier 3.6.2 everywhere). **POST-commit suite: app 203/203, ext 15/15.** Codegen lesson recorded: emit formatted output via the API; scoped ignores don't survive root lint-staged.

## Lead amendment — `19b91ef` (fix(F-005) follow-up)

- 2-line-class correction of a defect THIS TRAIN introduced (not pre-existing debt, hence not a parked finding): F-005's regenerated `.env.example`, `ci-env.ts`, and vitest fixture documented `COUPONS_DATABASE_URL` with a Prisma-style `?schema=public` param the porsager `postgres` driver rejects (F-001 executor proved it empirically). Dropped the param at all 3 template sites + honest NOTE comment in .env.example. 94/94 green; full husky gate chain (incl. new knip+prisma) passed. Rationale logged: fixing our own regression ≠ drive-by; commit reuses the F-005 ID for trace.
