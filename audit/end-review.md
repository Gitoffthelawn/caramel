# Caramel audit fix-train — fresh adversarial end-review

Branch `audit/fixes-2026-07-10` @ `145fb10`. Base `9cac6fd` (parent of first fix, the "Stage 2 plans" commit). **21** fix commits, not 19 (immaterial). Fresh reviewer; built none of this.

**Input caveat (material):** `stage3-results.md`, `findings-post-fix.json`, and `e2e-validation.md` do **not exist** anywhere in the repo (tracked or untracked). So the builders' self-report, their commit map, and the Stage-4 onboarding trace are unavailable. I reconstructed the commit→finding map from git log and reviewed diffs directly against `findings.json` + plans — strictly more adversarial (no self-report to be misled by), but it means "logged as parked" claims can't be cross-checked, and F-010's onboarding validation can't be confirmed.

## Validation (ran myself)

- `pnpm test`: extension **15/15 green**; app **289 pass + 1 fail (290)**. The one fail is `coupon-constants.generated.test.ts` byte-identity — **confirmed pure CRLF**: `core.autocrlf=true`, **no `.gitattributes` anywhere**, committed blob is LF (0 CR), working tree is CRLF, coupon vocabulary otherwise byte-identical. Green (290) on LF/CI. This is exactly the "289+1 CRLF" the brief predicted.
- `pnpm -r run type-check`: **exit 0**. `pnpm lint:oxlint`: **exit 0**. `pnpm --filter caramel-app exec knip`: **exit 0**.
- Cross-cutting: **no files touched outside** the plans' scope roots (apps/, .github/, .husky/, root configs, audit/, README/RUNBOOK/local-dev). Tautology sample of 5 test files (handleRouteError, coupons-read-boundary, withRoute, env, repo-integrity): healthy density (6/20, 9/24, 22/41, 10/20, 2/3 tests/expects), **zero trivial `expect(true)` assertions**.

## Verdict table

| Finding                                                | Commit  | Verdict             | Sharpest concern                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------ | ------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001 coupons-DB blind/untyped                         | 0359cb4 | **PASS**            | health/db probes BOTH DBs distinctly; zod row-schemas + `parseCouponRows` throw "schema drift" (seen live in test); drift CI. Solid.                                                                                                                                                         |
| F-002 failures-as-success / Sentry bypass              | 014a55f | **PASS-WITH-NOTES** | Helper + honest decrypt (`throw DecryptError`) + extension `error` field across ~13 routes. **2 OAuth 500s still bypass Sentry**: `oauth/authorize:173` (console.error only), `extension/login:72` (no log, no Sentry). Matches the "two parked".                                            |
| F-003 public key gates mutation                        | 8231e1d | **PASS**            | Server-only `COUPONS_ADMIN_SECRET` bearer; single `isTrustedServer()` for auth+exemption; old literal `WXqEpm2uOV…` purged tree-wide; tests prove stale keys no longer exempt. Nit: plain `===`, not timing-safe.                                                                            |
| F-004 no test suite (false-green)                      | 517a1b1 | **PASS**            | Real vitest (290+15), turbo+CI wired; Vitest-4 fail-on-zero default satisfies "fail when 0 tests"; characterization pins for shared-utils + routes.                                                                                                                                          |
| F-005 no env contract                                  | e52dbee | **PASS**            | zod env module throws on invalid (eager singleton at boot), .env.example regenerated, ci-env, 11 importers.                                                                                                                                                                                  |
| F-006 coupon domain duplicated                         | 5777695 | **PASS**            | Single `coupons.ts` + generated extension constants (consumed via `window.CaramelCoupons`); no-raw-status gate; **zero app-side stragglers**; only popup presentation-copy tokens allowlisted (narrow, justified).                                                                           |
| F-007 no shared request pipeline                       | 125827b | **PASS-WITH-NOTES** | Declarative `withRoute` (CORS/rate-limit/origin/apiKey/body) across all routes; OAuth session centralized. **Deviation:** "route through better-auth session API" NOT done (infeasible in better-auth 1.5.3); raw-Prisma mint consolidated into one documented helper w/ delete-when marker. |
| F-008 1536-line god-module                             | 2d9b962 | **PASS**            | Split into 6 files, monolith gone; `pickBestMatch` + the 245-line `applyCoupon` loop **byte-for-byte identical** old↔new.                                                                                                                                                                   |
| F-009 blind CI guardrails                              | 4e681b9 | **PASS**            | All fossils deleted (cors/initMiddleware/middlewares/apiResponse) + removed from knip ignore (green & truthful); oxlint+size-limit+husky. Branch protection is a GitHub server setting — unverifiable from tree.                                                                             |
| F-010 onboarding docs wrong                            | 989fb6d | **PASS-WITH-NOTES** | READMEs/LOCAL-DEV rewritten, boilerplate replaced. **Stage-4 fresh-agent validation artifact absent** — docs rewritten but end-to-end path unverified by me.                                                                                                                                 |
| F-011 operability nets absent                          | ef85860 | **PASS-WITH-NOTES** | RUNBOOK, error.tsx+global-error.tsx, smoke.ts, OpenRouter `X-Request-Id` header, PG `application_name` for the DB hop (honest — no HTTP boundary to carry a trace). Vercel-cron: `automaticVercelMonitors` unset (defaults off) — sub-claim moot/thin evidence.                              |
| F-012 LLM surface no evals                             | e6700f9 | **PASS**            | Suite+scorers+fixtures+scoreboard; ai-evals.yml = path-filter + nightly cron + dispatch + auto-issue; zod output schema.                                                                                                                                                                     |
| F-013 `any` on exported surfaces                       | 48f5142 | **PASS**            | apiResponseNext/cryptoHelpers/decryptJsonData typed; `no-explicit-any: error` active.                                                                                                                                                                                                        |
| F-014 dependency health                                | 93cde28 | **PASS**            | **No downgrades**, **all deps pinned** (no floating), react-awesome-button removed, nested lockfile removed, CI audit gate + deps-pinned test.                                                                                                                                               |
| F-015 stray/broken artifacts                           | f34ceab | **PASS**            | verify_test_small3.txt removed, package→`caramel-app`, amazon.js ref gone (+file absent), prettierrc fixed, repo-integrity gate. `nul` already untracked at base (Windows device artifact).                                                                                                  |
| F-017 token budget (train-discovered)                  | 09d8dbb | **PASS**            | 600 **not hand-waved**: candidate table (120 RED / 400 timeout-risk / 600 green+latency-safe, 0 trunc @ ~2x headroom / 800 skipped-with-reason), eval-gated green×2, cost-measured; candidly discloses prod has no OPENROUTER_API_KEY.                                                       |
| **Amendments** 19b91ef,0a806fd,12d675f,145fb10,e947a1d | —       | **LEGITIMATE**      | Each fixes a real regression from an earlier train commit (postgres `?schema=` rejection, compose creds, e2e file inventory sync, doc sync, prettier-format of generated file). Small, targeted, traceable — **not scope laundering**.                                                       |

## Falsification attempts that FAILED (the evidence)

- **F-003:** grepped the whole HEAD tree for the old public key `WXqEpm2uOV…` and any `x-api-key`/`EXPIRE_SECRET` in extension runtime → **gone entirely**; rateLimit tests independently prove the stale keys no longer exempt.
- **F-008:** extracted 2 function bodies (incl. the 245-line apply loop) from `9cac6fd:shared-utils.js` and diffed vs the concatenated split → **byte-identical**.
- **F-006:** hunted predicate-site stragglers bypassing the domain module → **none app-side**; the only extension raw tokens are popup presentation sentences, narrowly allowlisted with a written rationale (no app-side equivalent to drift against).
- **F-014:** semver-compared every dep old-floor vs new-pin for a stealth downgrade or leftover `^`/`~` → **zero downgrades, zero floating**.
- **Scope creep:** diffed the full 9cac6fd..145fb10 name list against the plan scope union → **nothing outside expected roots**.
- **Tautology:** sampled 5 test files for assert-nothing tests → **none**.

## Residuals (tracked; none rise to REDO)

1. **No `.gitattributes` + `autocrlf=true`** makes the F-006 sync gate + `pnpm test` RED on any Windows checkout (CI/Linux green). e947a1d fixed the prettier-format mismatch but not the line-ending one. Fix: add `* text=auto eol=lf` (or at least `*.generated.js eol=lf`). Cheapest highest-value hardening.
2. **F-002:** two OAuth 500 catch-sites (`authorize:173`, `login:72`) still bypass Sentry — real auth-failure blind spots; `login:72` doesn't even console.error.
3. **F-007:** raw-Prisma session mint survives (consolidated + documented as a better-auth 1.5.3 limitation).
4. **F-003 nit:** secret compared with `===`, not `timingSafeEqual`.
5. **Missing artifacts:** builder self-report / commit map / onboarding trace absent — provenance & F-010 end-to-end validation unverifiable.

## Overall ruling

**SHIP.** All 16 findings land — 11 PASS, 5 PASS-WITH-NOTES, **zero REDO**; amendments are legitimate regression fixes; type-check/oxlint/knip clean; the lone RED is a confirmed CRLF environment artifact, not a regression. Add a `.gitattributes` and file the two F-002 Sentry gaps as follow-ups.
