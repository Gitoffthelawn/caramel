# PLAN-F-010 — Onboarding docs are wrong/missing

**Finding:** F-010 (High, docs) · **Effort:** M · **Wave:** 4 · **Sequence:** 15 (DEAD LAST, before Stage-4 codify)
**Depends-on (must be landed & re-verified, not trusted):** F-004 (real `pnpm test`), F-005 (zod env module + regenerated `.env.example`), F-001 (coupons-DB health + drift + read boundary), F-011 (RUNBOOK.md), F-012 (`pnpm eval`), F-015 (package renamed `caramel-app`, stray artifacts removed).

## Executive summary

Rewrite the four onboarding docs so a docs-only engineer reaches a working system: root `README.md` (add real Getting Started, fix layout table), `local-dev/LOCAL-DEV.md` (reconcile ports/DBs/migrations/env), and replace both boilerplate app READMEs with ~15-line pointers. Pure docs — **zero code/config edits**, breaking = **N**. ~4 files modified, 0 deleted. Riskiest step: describing the coupons second-DB/migration reality _as it actually landed post-F-001_, not as this plan guesses — so Step 0 makes the executor VERIFY every future-tree claim against the real tree first. Validation is the test: a cold Haiku must onboard from docs alone with zero tribal gaps.

## Scope

- **Modify** `README.md` (root) — add Getting Started; fix `## Project layout` table paths (add `apps/` prefix); fix Safari-section paths + drop/repair dead `caramel-extension/scripts/README.md` link (verified absent); keep marketing intro, badges, CI/CD, License.
- **Modify** `local-dev/LOCAL-DEV.md` — correct the false "no host ports" claim (compose publishes `127.0.0.1:58005/58006` unconditionally; only `--remove-orphans` differs), add env-files model, both-DBs topology, migration step, connection strings, troubleshooting.
- **Replace** `apps/caramel-app/README.md` — delete create-next-app boilerplate + garbled `#  c a r a m e l - l a n d i n g` artifact (line 37); write ~15-line app pointer.
- **Replace** `apps/caramel-extension/README.md` — delete stray-quoted one-liner; write ~15-line extension pointer (load-unpacked + web-ext).
- **OUT of scope (name as new findings if still present):** editing `docker-compose.yml` to add a real no-port profile (OB-3 alt — behavior change); deleting `local-dev/verify_test_small3.txt` (OB-13 — F-015's artifact sweep; Step 0 verifies it's gone); CLAUDE.md/DESIGN.md (Stage-4, Fable); RUNBOOK.md _content_ (F-011 — link only); `.env.example` var-set (F-005 owns it — this plan consumes it); marketing copy.

## Approach

Single source of truth per fact; **README = linear quickstart, LOCAL-DEV = deep local-infra reference.** Split rule (state it verbatim in both docs):

- README Getting Started owns: prereqs, install, the `cp .env.example .env` step + the **secrets-provenance table** (where each secret comes from, human-only marked), the one migration command, the one dev command, the app URL, test/eval commands, repo-layout map, links out.
- LOCAL-DEV owns: the port-range table (authoritative, cites `local-dev/.env.ports`), how `.env.ports` (committed, ports) + `apps/caramel-app/.env` (gitignored, secrets) combine, the two-database topology + coupons degraded-mode, migration detail (reset/drift), connection strings, troubleshooting. LOCAL-DEV does **not** re-list secrets (points to README's table); README cites LOCAL-DEV as authoritative for ports/DB internals rather than restating them.
- **Rejected:** (a) one mega-README — buries infra detail, duplicates LOCAL-DEV; (b) duplicating the secrets table in both — guarantees drift (the exact class of bug this finding is); (c) adding a compose profile / seeding a local `caramel_coupons` to "fix" degraded mode — behavior change, out of docs scope, becomes a new finding.

## Sequencing (each step ends with its check)

0. **Landed-tree verification (do FIRST; every later claim is written from what you OBSERVE here, never from this plan's snapshot).** Record actual values for: package name in `apps/caramel-app/package.json` (expect `caramel-app` post-F-015); regenerated `apps/caramel-app/.env.example` var set + values (expect DB port `58005`, base URLs `58000`, and `COUPONS_DATABASE_URL`/`UPKUMA_HEALTH_SECRET`/`OPENROUTER_API_KEY` present); `pnpm test` output (expect real vitest, non-empty); RUNBOOK.md path (F-011); `pnpm eval` script + its env need (F-012); `couponsDb.ts` boundary behavior — does missing `COUPONS_DATABASE_URL` still `throw` at import (→ coupon routes 500) or degrade gracefully (F-001)?; coupons health-check route + drift-script names (F-001); that `local-dev/verify_test_small3.txt` is gone (F-015). **Check:** a written table of observed-value → each doc claim; any expectation that failed is flagged, and the doc is written to the OBSERVED reality (if a dependency didn't land as assumed, document truthfully and note it — do not invent).
1. **Determine the real migration command** against a fresh compose Postgres from empty: try `pnpm --filter caramel-app db:migrate:deploy` (applies the 3 committed migrations, no shadow DB, no prompt) and `db:migrate` (migrate dev). **Check:** pick the one that applies all migrations with zero prompts/errors on an empty DB; that exact command is what README documents (default: `db:migrate:deploy`).
2. **Rewrite root `README.md` Getting Started** (new section after the intro/badges, before or merged with Project layout): prereqs (Node 20+, pnpm 9 — match `packageManager`, docker); `pnpm install`; `cp apps/caramel-app/.env.example apps/caramel-app/.env` + secrets-provenance table; `pnpm dev:compose`; the Step-1 migration command; `pnpm dev` (note: also launches the extension's web-ext Chromium — app-only = `pnpm --filter caramel-app dev`); open `http://localhost:58000`; `pnpm test` (real, F-004) + `pnpm --filter caramel-app test:e2e` (Playwright — needs migrations applied) + `pnpm eval` (needs `OPENROUTER_API_KEY`, F-012); ~10-line repo-layout map; links to `local-dev/LOCAL-DEV.md` + RUNBOOK.md. **Check:** every command copy-runs; every referenced path exists; no `localhost:3000`/`2345` survives.
3. **Fix `README.md` Project layout table + Safari section** — prefix `apps/` on `caramel-app`, `caramel-extension`, `caramel-extension/scripts`, `caramel-extension/apple-extension`; verify each path exists; repair or remove the dead `scripts/README.md` reference. **Check:** every table/prose path resolves on disk.
4. **Secrets-provenance table (in README)** — from the Step-0 landed `.env.example`, classify each var: _local-compose-provided_ (`DATABASE_URL` → `postgresql://caramel:caramel_password@127.0.0.1:58005/caramel`); _generate-locally_ (`JWT_SECRET`, `BETTER_AUTH_SECRET` — random); _local-default_ (`BETTER_AUTH_URL`/`NEXT_PUBLIC_BASE_URL` → `http://localhost:58000`, `OPENROUTER_MODEL`, `NODE_ENV`); _human-only external, optional for basic boot_ (`GOOGLE_*`, `APPLE_*` provider dashboards; `NEXT_PUBLIC_SENTRY_DSN`; `USESEND_API_KEY`; `OPENROUTER_API_KEY` — required only for the cart classifier + `pnpm eval`); _external / unavailable locally_ (`COUPONS_DATABASE_URL` — owned by the out-of-repo Python service, **not** provisioned by local compose); _local-optional_ (`UPKUMA_HEALTH_SECRET` — set any value to use `/api/health/db`). **Check:** every var in the landed `.env.example` appears exactly once with a provenance and a human-only marker.
5. **Rewrite `local-dev/LOCAL-DEV.md`** — keep/verify the port table (58000 app / 58005 pg / 58006 redis, from `.env.ports`); correct the "no host ports" falsehood; add the two-env-files model; add the **two-database topology**: compose provisions only the `caramel` auth DB (Postgres) + Redis; the `caramel_coupons` DB is external/Python-owned and **not** available in local docs-only dev → describe the _observed_ degraded mode from Step 0 (coupon routes error/return-empty; homepage + auth work); add the migration step + reset/drift notes; connection strings; troubleshooting. **Check:** every fact matches Step-0 observations; no secret list duplicated here.
6. **Replace `apps/caramel-app/README.md`** (~15 lines): what it is (Next.js 16 App-Router web app + API for grabcaramel.com; better-auth + Prisma auth DB; coupon reads from the external `caramel_coupons` DB); key scripts (`dev`, `build`, `db:migrate*`, `test`, `test:e2e`, `knip`); "setup lives in the root README Getting Started" + links to LOCAL-DEV + RUNBOOK. **Check:** no create-next-app text or garbled artifact remains; ≤~20 lines.
7. **Replace `apps/caramel-extension/README.md`** (~15 lines): what it is (plain-JS MV3 extension, **no bundler** — files load verbatim; `manifest.json` + `manifest-firefox.json`); dev = `pnpm --filter caramel-extension dev` (web-ext Chromium) or load-unpacked (`chrome://extensions` → Developer mode → Load unpacked → `apps/caramel-extension`); `build` (rsync→`dist`), `package` (zip), `test:e2e` (`node scripts/test-extension.mjs`); link to root README + the Safari-icons note. **Check:** valid Markdown; no stray quote chars.
8. **Executor pre-flight (self-run, before requesting validation):** in a clean shell + clean clone state, follow the NEW docs **verbatim** — install → `cp .env.example .env` + fill per the table → `pnpm dev:compose` → migrate → `pnpm dev` → `GET http://localhost:58000` = 200 → run the documented test command → make a one-line comment change through the documented gates (`pnpm lint-staged`, `pnpm -r run type-check`) → revert → `docker compose down` → clean tree. **Check:** zero steps required knowledge outside the docs; fix docs and repeat until the executor's own verbatim run is clean.

## Breaking changes

None — docs only; no script, config, or code path changes. No consumer of any code artifact is affected. (If Step 0 finds a dependency did **not** land as assumed, that is a sequencing problem to escalate, not a reason to change code here.)

## Test strategy (validation IS the test — docs ship only when a cold agent passes)

- **Baseline (pinning equivalent):** `audit/empirical-onboarding.md` is the BEFORE trace (5 tribal-knowledge gaps: apps/ layout, `caramel-landing` package name, required-but-undocumented migrations, undocumented `caramel_coupons` DB/Python service, undocumented `UPKUMA_HEALTH_SECRET`; plus port/base-URL drift). Each MUST be doc-covered after.
- **Pre-flight gate:** Step 8 (executor's own verbatim run) must be clean first — never request validation on docs the author hasn't executed.
- **Cold-agent trace protocol (post-fix):** spawn a fresh **zero-context Haiku**, persona = brand-new engineer, knowledge = repo docs only (`README.md`, `local-dev/*.md`, both app READMEs, RUNBOOK.md) + generic Next/pnpm/Docker/Prisma conventions. Ground rules mirror `empirical-onboarding.md`: `pnpm install` pre-done, real commands executed, compose torn down at end, tree left clean, no secrets committed. Goals: (1) boot the app from docs alone to HTTP 200 on the **documented** port; (2) find + run the real test command; (3) make a one-line change through the documented gates; (4) report every step where a doc was missing/wrong (a new TRIBAL-x).
- **Pass criteria (all required):** (a) **zero new TRIBAL-x** — every action was doc-directed; (b) all 5 baseline tribal gaps are now doc-covered; (c) app reaches HTTP 200 on the documented port (58000), no dead-port surprise; (d) the coupons degraded-mode is documented so the agent is not surprised by empty/erroring coupon routes; (e) `pnpm test` behavior matches what the docs claim (real vitest, F-004); (f) the one-line-change-through-gates goal passes. Any failure → docs iterate, re-run the cold trace.

## Rollback

Four independent doc files; commit the whole fix as one commit `fix(F-010): rewrite onboarding docs to post-fix reality`. Internal checkpoints = per-file (Steps 2-3 README, 5 LOCAL-DEV, 6 app, 7 extension) so a failed cold-trace edit re-runs without losing prior files. Safe revert: `git revert` the single commit — docs-only, no runtime impact, no data/state.

## Risk

- **Blast radius:** zero runtime; worst case is a doc that still misleads → caught by the cold-agent gate before ship (the gate is the mitigation).
- **Premises: all verified in code (no re-scope).** Confirmed: README has no Getting Started + never links LOCAL-DEV (headings enumerated); layout table lacks `apps/` prefix (`README.md:47-50`); `dev` runs only `prisma generate` (`apps/caramel-app/package.json:6`); coupons DB undocumented + compose provisions only `caramel` (`docker-compose.yml:7-23`, `couponsDb.ts:3`); `.env.example` port `2345` vs `.env.ports` `58005`; app README boilerplate + garbled artifact (`:37`); extension README one stray-quoted line; "no host ports" false; root README cites a non-existent `caramel-extension/scripts/README.md`; 3 committed migrations exist; 10 route files import `couponsSql`.
- **Assumed (must be re-verified by Step 0, NOT trusted):** every dependency's landed shape — F-005 `.env.example` contents, F-004 `pnpm test`, F-001 coupons boundary/health/degraded behavior, F-011 RUNBOOK path, F-012 `pnpm eval`, F-015 rename + artifact removal. Early-warning sign: any Step-0 expectation that fails means a doc claim would be fiction — stop and document the observed reality (or escalate if a required dependency is missing).
